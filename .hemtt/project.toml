name = "GC Website Functions Client"
prefix = "gc"
author = "W-Cephei"



key_name = "{{prefix}}_{{version}}"


releasebuild = [
    "@zip gc_{{semver.major}}.{{semver.minor}}.{{semver.patch}}.{{semver.build}}"
]

[lint.sqf]
enabled = true
